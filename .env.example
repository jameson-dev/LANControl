# LANControl Configuration
# Copy this file to .env and customize for your environment

# Flask secret key (generate a secure random key)
# Generate with: python3 -c 'import secrets; print(secrets.token_hex(32))'
SECRET_KEY=change-this-to-a-random-secret-key

# Server binding configuration
BIND_HOST=127.0.0.1  # Use 0.0.0.0 to allow access from network
BIND_PORT=5000       # Port to listen on

# Default network scan range (CIDR notation)
DEFAULT_SCAN_RANGE=192.168.1.0/24

# Note: The install.sh script will automatically create a .env file
# with secure random keys and your chosen configuration
